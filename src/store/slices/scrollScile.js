import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const scrollScile = createSlice({
  name: "scrollScile",
  initialState,
  reducers: {},
});

export const {} = scrollScile.actions;

export default scrollScile.reducer;
